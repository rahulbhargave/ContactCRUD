(function() {

	var httpd = require( './index.js' );

	var server = new httpd();
	httpd.environ( 'root' , '../../public' ); 

	server.use(function( request , response , data ) {
		console.log(data);
	});

	server.start();

}());